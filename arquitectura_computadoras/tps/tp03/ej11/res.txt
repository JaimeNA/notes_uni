Lo que hace el algoritmo es recibir un arreglo de caracteres null terminates por parametro y lo pasa a mayuscula.

Imprime HELLO WORLD

Pasado a C:

void magic(char* a) {
	while(*a != 0) {
		if (*a > 96 && a <= 122)
			*a -= 32;

		a++;
	}
}
